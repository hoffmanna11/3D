package HUDs;

import java.awt.Graphics;

import game.HUD;

public class BaseVectors extends HUD {

	public void render(Graphics g) {
		// Get the base point to draw from
		// 100, 100
		// From that point, we'll draw the lines
	}
	
}
